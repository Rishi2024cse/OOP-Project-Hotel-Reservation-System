# Server Configuration
server.port=8082
server.servlet.context-path=/api

# Supabase Database Configuration
# Using the connection details from your Supabase dashboard
spring.datasource.url=jdbc:postgresql://db.qtcepllbrojadwhvqhtb.supabase.co:5432/postgres
spring.datasource.username=postgres
spring.datasource.password=rishi@bookify
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# JWT Configuration
jwt.secret=mySecretKey
jwt.expiration=86400000

# CORS Configuration
cors.allowed-origins=http://localhost:8080,http://localhost:8083,http://localhost:8081,http://localhost:5173,http://localhost:3000
cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
cors.allowed-headers=*
cors.allow-credentials=true
